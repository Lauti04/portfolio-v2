[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback so deep links resolve to the app entry point.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
